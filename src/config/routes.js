import { path } from '~/utils';

const routes = {
    home: path.HOME,
    homepage: path.HOMEPAGE,
    dashboard: path.DASHBOARD,
    devicemonitor: path.DEVICEMONITOR,
    monitorcontroler: path.MONITORCONTROLER,
    settings_controler: path.SETTINGS_CONTROLER,
    profileUser: path.PROFILE_USER,
    login: path.LOGIN,
    register: path.REGISTER,
    monitorRoom: path.MONITOR_ROOM,
    manage_account: path.MANAGE_ACCOUNT,
    devicemanager: path.DEVICE_MANAGER,
    addDevice: path.ADD_DEVICE,
    verifyemail: path.VERIFY_EMAIL,
    other: path.OTHER,
};

export default routes;
